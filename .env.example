# =============================================================================
# Jitsi Meet Docker Configuration
# =============================================================================
# Copy this file to .env and update the values according to your environment
# Command: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Basic Configuration
# -----------------------------------------------------------------------------

# Public URL for accessing Jitsi Meet
# For local development: http://localhost:8000
# For production: https://meet.yourdomain.com
PUBLIC_URL=http://localhost:8000

# Timezone for the application
# Find your timezone: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=UTC

# -----------------------------------------------------------------------------
# Port Configuration
# -----------------------------------------------------------------------------

# HTTP port for web interface (default: 8000)
HTTP_PORT=8000

# HTTPS port for web interface (default: 8443)
HTTPS_PORT=8443

# -----------------------------------------------------------------------------
# SSL/TLS Configuration
# -----------------------------------------------------------------------------

# Enable Let's Encrypt for automatic SSL certificates (0=disabled, 1=enabled)
# Note: Requires a valid domain name and ports 80/443 accessible from internet
ENABLE_LETSENCRYPT=0

# Enable HTTP to HTTPS redirect (0=disabled, 1=enabled)
ENABLE_HTTP_REDIRECT=0

# -----------------------------------------------------------------------------
# Authentication Configuration
# -----------------------------------------------------------------------------

# Enable authentication for creating rooms (0=disabled, 1=enabled)
# When enabled, only authenticated users can create new rooms
ENABLE_AUTH=0

# Enable guest access (0=disabled, 1=enabled)
# When enabled, guests can join rooms created by authenticated users
ENABLE_GUESTS=1

# -----------------------------------------------------------------------------
# Internal Service Passwords
# -----------------------------------------------------------------------------
# IMPORTANT: Change these passwords in production!
# Generate strong passwords using: openssl rand -hex 16
# -----------------------------------------------------------------------------

# Jicofo (Jitsi Conference Focus) authentication password
JICOFO_AUTH_PASSWORD=changeme_jicofo_password

# JVB (Jitsi Videobridge) authentication password
JVB_AUTH_PASSWORD=changeme_jvb_password

# Jigasi (SIP gateway) XMPP password
# Only needed if you plan to use SIP integration
JIGASI_XMPP_PASSWORD=changeme_jigasi_password

# Jibri (recording/streaming) XMPP password
# Only needed if you plan to use recording/streaming features
JIBRI_XMPP_PASSWORD=changeme_jibri_password

# Jibri recorder password
# Only needed if you plan to use recording features
JIBRI_RECORDER_PASSWORD=changeme_jibri_recorder_password

# -----------------------------------------------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------------------------------------------

# XMPP domain configuration (usually leave as default)
# XMPP_DOMAIN=meet.jitsi
# XMPP_AUTH_DOMAIN=auth.meet.jitsi
# XMPP_MUC_DOMAIN=muc.meet.jitsi
# XMPP_INTERNAL_MUC_DOMAIN=internal-muc.meet.jitsi

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# 1. For local development, you can use the default passwords
# 2. For production deployment:
#    - Change ALL passwords to strong, unique values
#    - Set PUBLIC_URL to your actual domain
#    - Enable LETSENCRYPT if using a real domain
#    - Consider enabling ENABLE_AUTH for better security
# 3. After changing this file, restart services: docker-compose down && docker-compose up -d
# =============================================================================

